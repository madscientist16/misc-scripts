#!/bin/bash

echo "BACKING UP"
restic-hdd backup --exclude-file /dir/restic-excludes.txt --files-from /dir/restic-includes.txt --skip-if-unchanged
echo ""
echo "REMOVING BACKUP SNAPSHOTS"
restic-hdd forget --keep-within-weekly 1m --keep-within-monthly 6m --prune && restic-hdd check
